[build]
builder = "dockerfile"

[deploy]
startCommand = "alembic upgrade head && python -c 'from app.main import app; print(\"app import OK\")' && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
