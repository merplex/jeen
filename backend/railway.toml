[build]
builder = "dockerfile"

[deploy]
startCommand = "timeout 20 alembic upgrade head; uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
