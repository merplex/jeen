version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: thaidict
      POSTGRES_USER: dictuser
      POSTGRES_PASSWORD: dictpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
